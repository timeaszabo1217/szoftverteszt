package tests.Client.performanceTests.controller;

import static org.junit.jupiter.api.Assertions.assertTrue;

/*
* Itt kellene folytatni a gondolatmenetet, hogy a compressedGame-et létrehozzuk, de akkor
* meg a playereket + mappot hozzuk létre.... stb.
* */

public class bad_GameControllerTest {
/*
    private GameController gameController;

    @BeforeEach
    public void setUp() {
        // Inicializáljuk a GameController példányt
        gameController = GameController.getInstance();

        // Létrehozunk egy teszt CompressedGame objektumot, és beállítjuk a GameController számára



        CompressedGame testGame = new CompressedGame();


        // Itt további inicializálást végezhetsz a testGame objektumon, ha szükséges
        gameController.setCurrentGame(testGame);
    }

    @Test
    public void testCalculateAvailableActionsPerformance() {
        // Start time measurement
        long startTime = System.nanoTime();

        // A metódus hívása, amelynek a teljesítményét tesztelni szeretnénk
        gameController.calculateAvailableActions();

        // End time measurement
        long endTime = System.nanoTime();

        // Calculate the execution time in milliseconds
        long executionTime = (endTime - startTime) / 1_000_000;

        // Print the execution time to the console
        System.out.println("Execution time: " + executionTime + " ms");

        // Assert that the execution time is under a certain threshold (e.g., 500 ms)
        assertTrue(executionTime < 500, "Method took too long to execute");
    }
*/
}
